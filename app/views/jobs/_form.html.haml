= form_for @job do |f|
  = f.text_field :name, class: 'form-control', placeholder: 'Add name here...'
  
  = f.text_field :frequency, class: 'form-control', placeholder:'Add how often the task needs to be done here...'

  = f.text_field :instructions, class: 'form-control', placeholder: 'Add instructions here...'  

  =f.submit "Save Job", class: 'btn btn-warning btn-block'
  %br


 